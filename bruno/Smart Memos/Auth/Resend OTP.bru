meta {
  name: Resend OTP
  type: http
  seq: 12
}

post {
  url: {{BASE_API_URL}}/auth/resend-otp
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john.doe@yopmail.com",
    "type": "PASSWORD_RESET"
  }
  // "PASSWORD_RESET" | "EMAIL_VERIFY" | "DELETE_ACCOUNT"
}

settings {
  encodeUrl: true
  timeout: 0
}
