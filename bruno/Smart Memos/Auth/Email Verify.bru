meta {
  name: Email Verify
  type: http
  seq: 2
}

post {
  url: {{BASE_API_URL}}/auth/verify-email
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john.doe@yopmail.com",
    "otp": "933432"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
